services:
  clinical-trial-agent:
    build: .
    image: clinical-trial-agent:latest
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./memory:/app/memory
      - ./vector_store:/app/vector_store
      - ./data:/app/data:ro
    restart: unless-stopped
